#ifndef _DSA_TYPES_HPP_
#define _DSA_TYPES_HPP_

#include <cstdint>

namespace dsa::types {

    using age_type = typename std::uint8_t;
    using count_type = typename std::uint8_t;
    using entry_type = typename std::uint16_t;

}

#endif
